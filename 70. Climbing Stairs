class Solution:
    def climbStairs(self, n: int) -> int:
        def count(ind):
            if ind==0:
                return 1
            elif ind==1:
                return 1
            return count(ind-1)+count(ind-2)
        return count(n)